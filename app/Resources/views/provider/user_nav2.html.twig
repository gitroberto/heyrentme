<div class="users-nav">
    <ul>
        <li><a href="{{ path('profil') }}" {% if page == 'profil' %}class="active"{% endif %}>Profil</a></li>
        <li><a href="{{ path('einstellungen') }}" {% if page == 'einstellungen' %}class="active"{% endif %}>Einstellungen</a></li>
        <li><a href="{{ path('fos_user_security_logout') }}">ausloggen</a></li>
        <li><a href="#" data-target="#delete-account" data-toggle="modal">Konto löschen</a></li>
        
        <li><a href="{{ path('dashboard') }}">zurück zu angebote</a></li>
    </ul>
</div>

    
<div id="delete-account" class="modal fade delete-offer" role="dialog">
  <div class="modal-dialog"> 
    <!-- Modal content-->
    <div class="modal-content">
      <button type="button" class="close" data-dismiss="modal"><img src="/img/close-modal.png" alt="close"/></button>
      <div class="content-modal">
        <p class="text-bold">ACHTUNG! Du bist dabei dein Konto zu löschen</p>
        <p class="delate-info">Möchtest du das wirklich? Dieser Schritt ist nicht rückgängig zu machen.</p>
        <a class="button btn-danger delete-btn" href="{{ path('delete-user') }}" >Konto endgültig löschen</a>
      </div>
    </div>
  </div>
</div>