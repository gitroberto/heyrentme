{% for eq in items %}
    <a class="text-black" href="{{ path('catchall', { 'content': eq.urlPath }) }}">
        <div class="col-md-4">
            {% if eq.equipmentImages|length > 0 %}
                <div style="height: 268px; overflow: hidden;">
                <img src="{{ eq.equipmentImages[0].image.urlPath(image_url_prefix) }}" alt="placeholder"/>
                </div>
            {% else %}
                <div style="height: 268px; overflow: hidden;">
                <img src="/img/equipment-default.jpg" alt="placeholder" style="max-height: 268px; overflow: hidden;"/>
                </div>
            {% endif %}
            {% if eq.priceBuy %}
                <img src="/img/cart.png" alt="cart" class="cart"/>
            {% endif %}
            {#
            {% if eq.activeDiscount %}
                <div class="discount"><span>-{{ "%d"|format(eq.activeDiscount.percent) }}%</span></div>
            {% endif %}
            #}
            <div class="product-info position-relative">
                <div class="row">
                    <img src="{{ eq.user.profilePicture(false, image_url_prefix) }}" alt="cart" class="product-user product-user2"/>
                    <div class="col-xs-7">
                        {{ include('common/stars.html.twig', { rating: eq.rating }) }}
                        <p class="product-name">
                            {{ eq.name }}
                        </p>
                    </div>
                    <div class="col-xs-5">
                        <p class="price">
                            {{ eq.activePrice|number_format(2, ',') }} &#8364;
                        </p>
                        <p class="tag">
                            pro {% if eq.subcategory.category.type == constant('AppBundle\\Entity\\Category::TYPE_EQUIPMENT') %}Tag{% else %}Stunde{% endif %}
                        </p>
                    </div>
                </div>
            </div>
            {% if eq.offerStatus %}
                <p class="product-list-info">{{ eq.offerStatus }}</p>
            {% endif %}
            <p class="product-list-info">{{ eq.description }}</p>
            <span class="list-desc-border"></span>
        </div>
    </a>
{% else %}
    <p>
        Keine Angebote gefunden - Wir arbeiten daran ;-)
    </p>
{% endfor %}
